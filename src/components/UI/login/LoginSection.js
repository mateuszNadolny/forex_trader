import LoginMessage from './LoginMessage';
import LoginModal from './LoginModal';

const LoginSection = () => {
  return (
    <>
      <LoginMessage />
      <LoginModal />
    </>
  );
};

export default LoginSection;
